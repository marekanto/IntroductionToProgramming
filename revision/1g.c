#include <stdio.h>
#include <stdlib.h>

int main()
{
    double a,b,c,d,x,y;
    max=a
    if(max<b)max=b;
    if(max<c)max=c;
    if(max<d)max=d;
    min=b
	if(min>a)min=a;
	if(min>c)min=c;
	if(min>d)min=d;
	if()
    
	printf("Hello world!\n");
    return 0;
}